#!/usr/bin/zsh

# $1 stack
# $2 account
# $3 app
# $4 log name

function omc-ips {
  omc status ${1} -a ${2} | tr -s ' ' | cut -d ' ' -f 6
}

omc-ips $1 $2 | parallel --line-buffer ssh dkendal@{} sudo tail -f /srv/www/${3}/shared/log/${4}
