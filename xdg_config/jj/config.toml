[user]
name = "Dylan Kendal"
email = "dylan.kendal@blvd.co"

[ui]
default-command = "log"
editor = "nvim"

[fix.tools.mix-format]
command = ["mix", "format", "--stdin-filename=$path", "-"]
patterns = ["glob:'**/*.ex'", "glob:'**/*.exs'"]


[merge-tools.hunk]
program = "nvim"
merge-args = [
    "-c :DiffEditor $left $right $output"
]

[merge-tools.nvim-diffconflicts]
program = "nvim"
merge-args = [
    "-c", "let g:jj_diffconflicts_marker_length=$marker_length",
    "-c", "JJDiffConflicts!", "$output", "$base", "$left", "$right",
]

[signing]
behavior = "own"
backend = "ssh"
key = "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAFiPjdvZtRVpfC/GVOeRE4vaBZjAejUMoEWPgkM1Ru/"
backends.ssh.program = "/Applications/1Password.app/Contents/MacOS/op-ssh-sign"

[commit]
gpgsign = true

[revset-aliases]
'user()' = 'user("dylan.kendal@blvd.co")'
'user(x)' = 'author(x) | committer(x)'
'HEAD' = '@-'
'closest_bookmark(to)' = 'heads(::to & bookmarks())'
'stack(to)' = 'mutable() & bookmarks() & ..to'
'stack()' = 'stack(@)'

[git]
sign-on-push = true

[template-aliases]
commit_description_verbose = '''
concat(
  description,
  "\n",
  "JJ: This commit contains the following changes:\n",
  indent("JJ:    ", diff.stat(72)),
  "JJ: ignore-rest\n",
  diff.git(),
)
'''

[aliases]
dv = ["--config=templates.draft_commit_description=commit_description_verbose", "describe"]
fetch = ["git", "fetch"]
push = ["git", "push"]
n = ["new", "-r", "trunk()"]
bl = ["bookmark", "list"]
tug = ["bookmark", "move", "--from", "closest_bookmark(@-)", "--to", "@-"]
gh = [
  "util",
  "exec",
  "--",
  "nu",
  "~/.config/jj/scripts/jj-gh.nu",
]
"gh-create" = [
  "util",
  "exec",
  "--",
  "nu",
  "~/.config/jj/scripts/jj-gh-create.nu",
]
"gh-sync" = [
  "util",
  "exec",
  "--",
  "nu",
  "~/.config/jj/scripts/jj-gh-sync.nu",
]
