complete -c stackprof -l help -d "Show help" # Usage: stackprof [file.dump]+ [--text|--method=NAME|--callgrind|--graphviz] command -c stackprof -l text -d "Text summary per method (default)"
complete -c stackprof -l files -d "List of files"
complete -c stackprof -l limit -d "Limit --text, --files, or --graphviz output to N entries"
complete -c stackprof -l sort-total -d "Sort --text or --files output on total samples"
complete -c stackprof -l method -d "Zoom into specified method"
complete -c stackprof -l file -d "Show annotated code for specified file"
complete -c stackprof -l walk -d "Walk the stacktrace interactively"
complete -c stackprof -l callgrind -d "Callgrind output (use with kcachegrind, stackprof-gprof2dot.py)"
complete -c stackprof -l graphviz -d "Graphviz output (use with dot)"
complete -c stackprof -l node-fraction -d "Drop nodes representing less than [frac] fraction of samples"
complete -c stackprof -l stackcollapse -d "stackcollapse.pl compatible output (use with stackprof-flamegraph.pl)"
complete -c stackprof -l flamegraph -d "timeline-flamegraph output (js)"
complete -c stackprof -l flamegraph-viewer -d "open html viewer for flamegraph output"
complete -c stackprof -l select-files -d "Show results of matching files"
complete -c stackprof -l reject-files -d "Exclude results of matching files"
complete -c stackprof -l select-names -d "Show results of matching method names"
complete -c stackprof -l reject-names -d "Exclude results of matching method names"
complete -c stackprof -l dump -d "Print marshaled profile dump (combine multiple profiles)"
complete -c stackprof -l debug -d "Pretty print raw profile data"
